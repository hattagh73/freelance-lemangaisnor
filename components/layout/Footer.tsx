import classNames from 'classnames';

//* START: footer
const Footer = () => {
    return (
        <footer
            className={classNames(
                "c-container"
            )}
        >
            this is a footer
        </footer>
    )
}
export default Footer;
//* END: footer