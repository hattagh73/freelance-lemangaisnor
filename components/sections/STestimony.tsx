// import { iHero } from '../../config/data-type';

import classNames from 'classnames';

//* START: section-testimony
const STestimony = () => {
    return (
        <section
            id="s_testimony"
            className={classNames(
                "c-container h-[80vh]",
            )}
        >
            This is a testimony section
        </section>
    )
}
export default STestimony;
//* END: section-testimony